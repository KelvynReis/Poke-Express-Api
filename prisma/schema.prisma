generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Pokemon {

  id          String @id @default(uuid())
  name        String
  description String
  type        String


  @@map("pokemons")
}
